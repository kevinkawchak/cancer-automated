% Abstract and Bill Synopsis: one paragraph, under 250 words, no citations.
% Built by the future pass from the instruction below. Do not process now.

[Write a single-paragraph abstract of under 250 words, with no citations (cite in
the body, not here). Open by naming the instrument: the VVUQ Physical AI Oncology
Trial Bill, a proposed United States law requiring an automated verification,
validation, and uncertainty quantification (VVUQ) process to clear robot-patient
interaction code before that code is generated or executed in a Physical AI
oncology clinical trial. State the one-sentence thesis: rigor over the code
itself, where verification ahead of generation is the patient-safety and efficacy
control, and where repository-based AI models can synthesise VVUQ code generation
and execution into public documents that improve and accelerate the legislative
process. Then summarise the evidentiary spine in three strokes, pulling the
headline numbers (not the file names) from the records named in
\texttt{sections/findings.tex}: (1) two author VVUQ studies show large language
models are appropriate for code verification, the first proving the gate, not
generation, is the decision-bearing work (51 of 51 tests; one ACCEPT against five
BLOCK and one ESCALATE), the second proving practical VVUQ code generation and
execution against fourteen external standards and two clinical baselines (172 of
172 tests, 64 in the ten-gate assurance suite, a 1790-line four-entrant
comparison, and a 1001-row non-repetitive humanoid sensor stream, both reproduced
byte for byte from seed 20260525); (2) the national platform supplies the USL and
PSL standards and five trial, sponsor, and mobile simulations that make the
robot-patient case concrete; and (3) the bill binds these to current federal and
state law. Close on the policy claim: codifying verification before generation
keeps the United States first in surgical-humanoid VVUQ while making oncology
trials faster, less expensive, and safer. Keep the Unitree H2-Surgical 1.0
labelled hypothetical and every supporting number a simulation result. No images.]
