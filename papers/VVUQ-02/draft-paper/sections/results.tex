% Results: the 10-gate decision surface (centerpiece), the pipeline behaviors, the
% 32-iteration sweep and the 1790-line tournament, the featured 1000-row sensor
% stream, the 60-second timeline and safety surface, and the determinism result.
% Built by the future pass from the instructions below. Do not process now.
% Figure 03 (threshold forest) and Figure 11 (sensor safety bands) are placed
% here; \autoref both from the prose.

[Write Results as connected subsections, leading with the decision surface because
it is the operational proof of the thesis. Carry the dense numbers in left-aligned
fixed-width tables (\texttt{\{>\{\textbackslash raggedright\textbackslash
arraybackslash\}p\{width\}\}} columns); reference each table and figure with
\autoref and do not restate values already shown in a table.]

[Subsection 1, the ten-gate decision surface (centerpiece). From
\texttt{papers/VVUQ-02/execution/03-vvuq/README.md} and its artifacts
\texttt{gate\_decision\_surface.txt}, \texttt{vvuq\_report.md}, and
\texttt{vvuq\_decisions.json} (all under
\texttt{papers/VVUQ-02/execution/03-vvuq/artifacts/}), report the five executed
cases: the nominal sweep in which all ten gates ACCEPT over their independent
references, and four adversarial cases proving the failing paths fail, namely a
catastrophe hard-predicate BLOCK on gate 06, a verification-fraction BLOCK at 0.80
on gate 08, a validation-agreement BLOCK on gate 03, and an uncertainty ESCALATE
at coefficient of variation 0.163 on gate 02 that defaults to hand-back-to-human.
Present the five-case decision-surface summary as a table. Introduce
\autoref{fig:forest} here: explain that the ten gate thresholds tighten with model
risk toward the three immediate-catastrophe gates, and present the threshold table
(verification fraction 1.0 for all, agreement, maximum relative error, and
coefficient-of-variation bound per gate) from the same README and from
\texttt{papers/VVUQ-02/codegen/config/vvuq\_thresholds.yaml}. Emphasise that
verification is pass or fail, not a weighted average, and that the harness blocks
or escalates on the first failing dimension.]

[Subsection 2, the executed control behaviors. From
\texttt{papers/VVUQ-02/execution/02-pipeline/artifacts/pipeline\_execution\_log.txt}
(indexed by \texttt{papers/VVUQ-02/execution/02-pipeline/README.md}), report the
six behavior-group results: phase-step concordance of 1.000 for the autonomy
intent and compile, the forward-kinematics tip pose for the right seven-DOF arm,
the smooth Dice degradation under occlusion with all five structures usable after
NIR, ultrasound, and bile fusion, the fingertip force tracking under the soft cap
with the bimanual sum within the hard cap, the zero-moment-point margins holding
across the disturbance range, and the three anastomoses holding ring tension in
band. Keep this subsection tight; it establishes the candidate behaviors that the
gates then judge, which is the thesis in miniature (generation is cheap, assurance
is the work).]

[Subsection 3, the 32-iteration sweep and the four-entrant tournament. This
subsection must feature the significant code generation feat and its execution.
From \texttt{papers/VVUQ-02/execution/04-automation/README.md} and its artifacts
\texttt{sweep\_index.jsonl} and \texttt{composite\_scores.jsonl} (under
\texttt{papers/VVUQ-02/execution/04-automation/artifacts/}), report that all 32 of
32 iterations cleared all ten gates with the composite spanning min 93.417, max
93.715, and mean 93.562. Then feature the 1790-line, four-entrant results file
\texttt{papers/VVUQ-02/codegen/results/comparison.json}, generated by
\texttt{papers/VVUQ-02/codegen/src/llm/compare\_agent.py} and processed in
execution as recorded in
\texttt{papers/VVUQ-02/execution/04-automation/artifacts/comparison\_analysis.txt}
and summarised in \texttt{comparison\_leaderboard.md}: 32 iterations times four
rounds, 128 verdicts, 100 percent caveat coverage, and the leaderboard over the
H2-Surgical 1.0, the eight-arm PancreSpeed 1.0, a teleoperated da Vinci-successor,
and the 2025 Dutch human-surgeon baseline. Present the leaderboard (entrant,
composite mean, win rate, total wins) as a table read directly from the
\texttt{leaderboard} array of \texttt{comparison.json}; state plainly that every
round carries the hypothetical-2030 simulation-against-simulation caveat. Make the
generation-versus-assurance point explicit: producing a 1790-line structured
tournament file is fast and deterministic, while judging it (caveat coverage, win
accounting, dispersion) is the assurance work.]

[Subsection 4, the featured humanoid sensor stream. This subsection must feature
the substantial code generation and execution of the robot positional data. From
\texttt{papers/VVUQ-02/codegen/data/sample\_h2\_sensor.csv}, generated by
\texttt{papers/VVUQ-02/codegen/src/sensors/ingest\_h2.py}, and the analysis in
\texttt{papers/VVUQ-02/execution/05-humanoid-deployment/artifacts/sensor\_stream\_analysis.txt}
(indexed by \texttt{papers/VVUQ-02/execution/05-humanoid-deployment/README.md}),
report that the committed stream is 1000 unique rows across 27 columns: 500 command
ticks for each of the two hands, carrying seven arm-joint channels, five fingertip
force channels, and three end-effector position channels per hand, plus the safety
channels (vessel proximity, ring tension, support-polygon margin, balance state,
forces, e-stop, collision). State that every row and every positional payload is
distinct with zero duplicates, and that the sample is the first 50 ms of phase 1 at
the 10 kHz command rate. Introduce \autoref{fig:bands} here: it plots three
safety-relevant channels (summed two-hand force against its soft and hard caps,
support-polygon margin far above the stability floor, and fingertip distance at or
above the vessel no-fly radius) with mean plus or minus one standard deviation
bands. Present the per-channel statistics (min, max, mean, sd, bound, gate) as a
table read from the figure-11 grounding in
\texttt{papers/VVUQ-02/image-instruct/11-sensor-stream-safety-bands/README.md} and
the analysis artifact. Emphasise that generating 1000 non-repetitive rows of
hands, five fingers, and arm data is cheap, while verifying their uniqueness and
safety-channel ranges is the assurance work.]

[Subsection 5, the 60-second Whipple timeline and the safety surface. From
\texttt{papers/VVUQ-02/execution/05-humanoid-deployment/artifacts/eight\_phase\_timeline.txt}
and \texttt{deployment\_safety\_log.txt}, report the eight-phase 60-second
timeline and the three catastrophe-gate safety paths (vascular no-fly, shared-OR
collision, fault e-stop) all behaving correctly at their boundaries. Keep this
brief and reference the safety bounds already shown in \autoref{fig:bands}.]

[Subsection 6, determinism. Close Results with the byte-for-byte reproducibility
result from \texttt{papers/VVUQ-02/execution/01-foundation/environment-and-verification.md}:
the sensor CSV byte-identical, the comparison a semantic match, and the sweep index
identical, all from seed 20260525. State that this is what lets every number above
stand as reproducible evidence rather than a trusted summary.]

\begin{figure}[H]
   \centering
   \IfFileExists{Images/03-ten-gate-threshold-forest.png}{%
     \hspace{-0.95cm}%
     \includegraphics[width=1.1\linewidth, trim={0 0 0 0}, clip]{Images/03-ten-gate-threshold-forest.png}%
     % \vspace{-0.25cm}
   }{%
     \setlength{\fboxsep}{10pt}%
     \fbox{\parbox[c][6cm][c]{0.9\linewidth}{\centering\itshape Figure placeholder.
     The author adds \texttt{Images/03-ten-gate-threshold-forest.png}, rendered by
     \texttt{papers/VVUQ-02/imagegen/03-ten-gate-threshold-forest/03-ten-gate-threshold-forest.py}.}}%
   }
    \caption{Forest plot of the ten VVUQ gate thresholds, with bounds tightening toward the three immediate-catastrophe gates.}
    \label{fig:forest}
\end{figure}

\begin{figure}[H]
   \centering
   \IfFileExists{Images/11-sensor-stream-safety-bands.png}{%
     \hspace{-0.95cm}%
     \includegraphics[width=1.1\linewidth, trim={0 0 0 0}, clip]{Images/11-sensor-stream-safety-bands.png}%
     % \vspace{-0.25cm}
   }{%
     \setlength{\fboxsep}{10pt}%
     \fbox{\parbox[c][6cm][c]{0.9\linewidth}{\centering\itshape Figure placeholder.
     The author adds \texttt{Images/11-sensor-stream-safety-bands.png}, rendered by
     \texttt{papers/VVUQ-02/imagegen/11-sensor-stream-safety-bands/11-sensor-stream-safety-bands.py}.}}%
   }
    \caption{Live safety margins across the featured 1000-row humanoid sensor stream with mean and uncertainty bands against the governing gate bounds.}
    \label{fig:bands}
\end{figure}
