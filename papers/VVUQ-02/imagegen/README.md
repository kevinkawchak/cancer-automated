# VVUQ-02 imagegen (placeholder)

Reserved for the future figure outputs, one subdirectory per figure at
`imagegen/NN-name/NN-name.{py,png}`, generated from the specifications in
`papers/VVUQ-02/image-instruct/`. Each generator script will be ruff-clean and
self-contained, use `matplotlib.use("Agg")`, portrait `figsize=(8.5, 11)` at 300
dpi, white background only, the shared palette, and the section symbol U+00A7.

This directory is intentionally empty until the downstream image-generation pull
request lands.
